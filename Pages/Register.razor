@page "/register"
@inject PhilanthroPoints.Services.UserService UserSvc
@inject NavigationManager Nav
@using System.ComponentModel.DataAnnotations

<div class="register-container">
    <div class="register-card">
        <div class="register-header">
<<<<<<< HEAD
            <h2>🎉 Join PhilanthroPoints!</h2>
=======
            <h2>Join PhilanthroPoints!</h2>
>>>>>>> Mike's-Commits
            <p>Create your account to start planning amazing birthday celebrations</p>
        </div>

        <EditForm Model="newUser" OnValidSubmit="RegisterUser">
            <DataAnnotationsValidator />
            <ValidationSummary class="validation-summary" />

            <div class="form-grid">
                <div class="form-group">
                    <label for="username">Username *</label>
                    <InputText id="username" @bind-Value="newUser.Username" class="input" placeholder="Choose a username..." />
                    <ValidationMessage For="@(() => newUser.Username)" />
                </div>

                <div class="form-group">
                    <label for="email">Email Address *</label>
                    <InputText id="email" @bind-Value="newUser.Email" class="input" placeholder="your@email.com" />
                    <ValidationMessage For="@(() => newUser.Email)" />
                </div>

                <div class="form-group">
                    <label for="firstName">First Name *</label>
                    <InputText id="firstName" @bind-Value="newUser.FirstName" class="input" placeholder="Your first name..." />
                    <ValidationMessage For="@(() => newUser.FirstName)" />
                </div>

                <div class="form-group">
                    <label for="lastName">Last Name *</label>
                    <InputText id="lastName" @bind-Value="newUser.LastName" class="input" placeholder="Your last name..." />
                    <ValidationMessage For="@(() => newUser.LastName)" />
                </div>

                <div class="form-group">
                    <label for="phone">Phone Number</label>
                    <InputText id="phone" @bind-Value="newUser.PhoneNumber" class="input" placeholder="(555) 123-4567" />
                </div>

                <div class="form-group">
                    <label for="dateOfBirth">Date of Birth *</label>
                    <InputDate id="dateOfBirth" @bind-Value="newUser.DateOfBirth" class="input" />
                    <ValidationMessage For="@(() => newUser.DateOfBirth)" />
                </div>

                <div class="form-group">
                    <label for="address">Address</label>
                    <InputText id="address" @bind-Value="newUser.Address" class="input" placeholder="123 Main Street..." />
                </div>

                <div class="form-group">
                    <label for="city">City</label>
                    <InputText id="city" @bind-Value="newUser.City" class="input" placeholder="Your city..." />
                </div>

                <div class="form-group">
                    <label for="zipCode">Zip Code</label>
                    <InputText id="zipCode" @bind-Value="newUser.ZipCode" class="input" placeholder="12345" />
                </div>
            </div>

            <div class="child-info-section">
                <h3>🧒 Child Information (Optional)</h3>
                <p class="section-desc">Tell us about the birthday child to personalize your experience</p>
                
                <div class="form-grid">
                    <div class="form-group">
                        <label for="childName">Child's Name</label>
                        <InputText id="childName" @bind-Value="newUser.ChildName" class="input" placeholder="Child's name..." />
                    </div>

                    <div class="form-group">
                        <label for="childAge">Child's Age</label>
                        <InputNumber id="childAge" @bind-Value="newUser.ChildAge" class="input" min="0" max="18" />
                    </div>

                    <div class="form-group">
                        <label for="childGender">Child's Gender</label>
                        <InputSelect id="childGender" @bind-Value="newUser.ChildGender" class="input">
                            <option value="">-- Select --</option>
                            <option value="Male">Male</option>
                            <option value="Female">Female</option>
                            <option value="Other">Other</option>
                            <option value="Prefer not to say">Prefer not to say</option>
                        </InputSelect>
                    </div>

                    <div class="form-group">
                        <label for="childEthnicity">Child's Ethnicity</label>
                        <InputSelect id="childEthnicity" @bind-Value="newUser.ChildEthnicity" class="input">
                            <option value="">-- Select --</option>
                            <option value="African American">African American</option>
                            <option value="Asian">Asian</option>
                            <option value="Hispanic / Latino">Hispanic / Latino</option>
                            <option value="Native American">Native American</option>
                            <option value="Pacific Islander">Pacific Islander</option>
                            <option value="White">White</option>
                            <option value="Mixed">Mixed</option>
                            <option value="Other">Other</option>
                            <option value="Prefer not to say">Prefer not to say</option>
                        </InputSelect>
                    </div>
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary" disabled="@isSubmitting">
                    @if (isSubmitting)
                    {
<<<<<<< HEAD
                        <span>🔄 Creating Account...</span>
                    }
                    else
                    {
                        <span>🎉 Create Account</span>
=======
                        <span>Creating Account...</span>
                    }
                    else
                    {
                        <span>Create Account</span>
>>>>>>> Mike's-Commits
                    }
                </button>
                <button type="button" class="btn btn-secondary" @onclick="GoHome">Cancel</button>
            </div>
        </EditForm>

        @if (!string.IsNullOrEmpty(statusMessage))
        {
            <div class="status-message @statusClass">
                @statusMessage
            </div>
        }

        <div class="login-link">
            Already have an account? <a href="/">Sign in here</a>
        </div>
    </div>
</div>

@code {
    private User newUser = new();
    private bool isSubmitting = false;
    private string statusMessage = "";
    private string statusClass = "";

    protected override void OnInitialized()
    {
        newUser.DateOfBirth = DateTime.Now.AddYears(-25); // Default age
    }

    private async Task RegisterUser()
    {
        isSubmitting = true;
        statusMessage = "";
        StateHasChanged();

        try
        {
            // Validate required fields
            if (string.IsNullOrWhiteSpace(newUser.Username) || 
                string.IsNullOrWhiteSpace(newUser.Email) ||
                string.IsNullOrWhiteSpace(newUser.FirstName) ||
                string.IsNullOrWhiteSpace(newUser.LastName))
            {
                statusMessage = "Please fill in all required fields.";
                statusClass = "error";
                return;
            }

            // Check if username is available
            var usernameAvailable = await UserSvc.IsUsernameAvailableAsync(newUser.Username);
            if (!usernameAvailable)
            {
                statusMessage = "Username is already taken. Please choose a different one.";
                statusClass = "error";
                return;
            }

            // Check if email is available
            var emailAvailable = await UserSvc.IsEmailAvailableAsync(newUser.Email);
            if (!emailAvailable)
            {
                statusMessage = "Email is already registered. Please use a different email address.";
                statusClass = "error";
                return;
            }

            // Create the user
            var success = await UserSvc.CreateUserAsync(newUser);
            if (success)
            {
<<<<<<< HEAD
                statusMessage = "🎉 Account created successfully! Welcome to PhilanthroPoints!";
=======
                statusMessage = "Account created successfully! Welcome to PhilanthroPoints!";
>>>>>>> Mike's-Commits
                statusClass = "success";
                
                // Redirect to home page after a delay
                await Task.Delay(2000);
                Nav.NavigateTo("/");
            }
            else
            {
                statusMessage = "Failed to create account. Please try again.";
                statusClass = "error";
            }
        }
        catch (Exception ex)
        {
            statusMessage = $"An error occurred: {ex.Message}";
            statusClass = "error";
        }
        finally
        {
            isSubmitting = false;
            StateHasChanged();
        }
    }

    private void GoHome()
    {
        Nav.NavigateTo("/");
    }
}

<style>
.register-container {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    min-height: 100vh;
    padding: 2rem 1rem;
}

.register-card {
    background: white;
    border-radius: 20px;
    padding: 2rem;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
    max-width: 800px;
    width: 100%;
}

.register-header {
    text-align: center;
    margin-bottom: 2rem;
}

.register-header h2 {
    color: var(--bc-primary);
    margin-bottom: 0.5rem;
    font-size: 2rem;
}

.register-header p {
    color: #6b7280;
    font-size: 1.1rem;
}

.form-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
}

.form-group {
    display: flex;
    flex-direction: column;
}

.form-group label {
    font-weight: 600;
    color: var(--bc-dark);
    margin-bottom: 0.5rem;
}

.input {
    padding: 0.75rem;
    border: 2px solid #e5e7eb;
    border-radius: 8px;
    font-size: 1rem;
    transition: border-color 0.2s ease;
}

.input:focus {
    border-color: var(--bc-primary);
    outline: none;
    box-shadow: 0 0 0 3px rgba(36, 158, 160, 0.1);
}

.child-info-section {
    background: #f8fafc;
    border-radius: 12px;
    padding: 1.5rem;
    margin: 2rem 0;
}

.child-info-section h3 {
    color: var(--bc-primary);
    margin-bottom: 0.5rem;
}

.section-desc {
    color: #6b7280;
    margin-bottom: 1.5rem;
    font-style: italic;
}

.form-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-top: 2rem;
}

.btn {
    padding: 1rem 2rem;
    border: none;
    border-radius: 12px;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-primary {
    background: var(--bc-gradient);
    color: white;
    box-shadow: 0 4px 15px rgba(36, 158, 160, 0.4);
}

.btn-primary:hover:not(:disabled) {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(36, 158, 160, 0.5);
}

.btn-primary:disabled {
    opacity: 0.7;
    cursor: not-allowed;
    transform: none;
}

.btn-secondary {
    background: #6b7280;
    color: white;
}

.btn-secondary:hover {
    background: #4b5563;
    transform: translateY(-1px);
}

.status-message {
    padding: 1rem;
    border-radius: 8px;
    text-align: center;
    margin-top: 1rem;
    font-weight: 600;
}

.status-message.success {
    background: #ecfdf5;
    color: #065f46;
    border: 1px solid #a7f3d0;
}

.status-message.error {
    background: #fef2f2;
    color: #991b1b;
    border: 1px solid #fecaca;
}

.login-link {
    text-align: center;
    margin-top: 1.5rem;
    color: #6b7280;
}

.login-link a {
    color: var(--bc-primary);
    text-decoration: none;
    font-weight: 600;
}

.login-link a:hover {
    text-decoration: underline;
}

.validation-summary {
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #991b1b;
    padding: 1rem;
    border-radius: 8px;
    margin-bottom: 1rem;
}

@@media (max-width: 768px) {
    .form-grid {
        grid-template-columns: 1fr;
    }
    
    .form-actions {
        flex-direction: column;
    }
    
    .btn {
        width: 100%;
    }
}
</style>